(ns defender-cljs.canvas.hud
  "Provides an abstraction over Three.js for producing hud objects"
  (:require [defender-cljs.constants :as c]))

(def hud-map-width (/ c/view-width 3))

(defn draw-hud-layout []
  (let []))
